{
  "name": "fpl-recap",
  "version": "0.1.0",
  "description": "A single-page recap that scores our fantasy football predictions against what actually happened.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "ingest": "node scripts/ingest.js",
    "fetch:fpl": "node scripts/fetch-fpl.js"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@fontsource/archivo": "^5.3.0",
    "@fontsource/inter": "^5.3.0"
  }
}
